apiVersion: v1
kind: Service
metadata:
  name: netai-chatbot
  namespace: netai
  labels:
    app.kubernetes.io/name: netai-chatbot
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: netai-chatbot
