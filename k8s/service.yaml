apiVersion: v1
kind: Service
metadata:
  name: netai-chatbot-api
  labels:
    app: netai-chatbot-api
spec:
  type: ClusterIP
  selector:
    app: netai-chatbot-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
